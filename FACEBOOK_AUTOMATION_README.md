# Facebook Automation System

ระบบตอบคอมเมนต์และส่งข้อความอัตโนมัติพร้อม AI ที่มีบริบท สำหรับ Facebook Page

## 🎯 Features

- ✅ ตอบกลับคอมเมนต์อัตโนมัติ
- ✅ ส่งข้อความส่วนตัวทาง Messenger อัตโนมัติ
- ✅ AI Chatbot ที่เข้าใจบริบทจากโพสต์และคอมเมนต์
- ✅ Admin UI สำหรับตั้งค่าแต่ละโพสต์
- ✅ สถิติและ Logs การทำงาน
- ✅ รองรับลูกค้าเก่าที่เคยคุยกับ AI แล้ว

## 📋 การติดตั้ง

### 1. ติดตั้ง Dependencies

```bash
npm install
```

### 2. ตั้งค่า Environment Variables

เพิ่มตัวแปรต่อไปนี้ใน `.env`:

```env
# Facebook
FB_PAGE_ACCESS_TOKEN=your_page_access_token
FB_APP_SECRET=your_app_secret
FB_VERIFY_TOKEN=your_verify_token
FB_PAGE_ID=your_page_id

# MongoDB
MONGODB_URI=your_mongodb_uri

# OpenAI
OPENAI_API_KEY=your_openai_api_key

# Base URL
NEXT_PUBLIC_BASE_URL=https://your-domain.com
```

### 3. รัน Migration

Migration จะรันอัตโนมัติเมื่อ build แต่สามารถรันด้วยตัวเองได้:

```bash
npm run migrate:conversations
```

### 4. Build และ Deploy

```bash
npm run build
npm start
```

## 🔧 การตั้งค่า Facebook Webhook

### 1. ตั้งค่า Webhook URL

ไปที่ Facebook App Dashboard → Webhooks

- **Callback URL**: `https://your-domain.com/api/messenger/webhook`
- **Verify Token**: ใช้ค่าเดียวกับ `FB_VERIFY_TOKEN`

### 2. Subscribe to Events

เลือก Events ต่อไปนี้:

- ✅ `messages`
- ✅ `messaging_postbacks`
- ✅ `feed` (สำหรับ comments)

### 3. Subscribe Page

เชื่อม Webhook กับ Facebook Page ของคุณ

## 📖 การใช้งาน

### 1. เข้าสู่ Admin Panel

ไปที่ `/admin/facebook-automation`

### 2. เพิ่มโพสต์

โพสต์จะถูกเพิ่มอัตโนมัติเมื่อมีคนคอมเมนต์ หรือสามารถเพิ่มด้วยตัวเองผ่าน API:

```javascript
POST /api/admin/facebook-automation/posts
{
  "postId": "123456789",
  "message": "โปรโมชั่นพิเศษ! วิตามินซี...",
  "postType": "photo",
  "permalink": "https://facebook.com/...",
  "createdTime": "2025-01-01T00:00:00Z"
}
```

### 3. ตั้งค่า Automation

คลิกที่ปุ่ม "ตั้งค่า" ของโพสต์ แล้วกำหนด:

#### เปิดใช้งาน Automation
- เปิด/ปิดการทำงานอัตโนมัติ

#### ตอบกลับคอมเมนต์
- เปิด/ปิดการตอบกลับคอมเมนต์
- ตั้งข้อความตอบกลับ (ใช้ `{{name}}` สำหรับชื่อผู้คอมเมนต์)

#### ส่งข้อความส่วนตัว
- เปิด/ปิดการส่งข้อความทาง Messenger
- ตั้งข้อความต้อนรับ (ใช้ `{{name}}`, `{{comment}}`, `{{post_title}}`)

#### คำสั่งพิเศษสำหรับ AI
- บอก AI ว่าควรตอบอย่างไรเมื่อลูกค้าตอบกลับ
- ตัวอย่าง: "ตอบคำถามเกี่ยวกับโปรโมชั่นวิตามินซี ให้ข้อมูลราคาและส่วนลด"

#### ข้อมูลเพิ่มเติมสำหรับ AI
- ข้อมูลที่ AI ควรรู้เกี่ยวกับโพสต์นี้
- ตัวอย่าง: "สินค้า: วิตามินซี 1000mg, ราคา: 299 บาท (ลด 20%), จัดส่งฟรี"

### 4. ทดสอบ

1. ไปที่โพสต์บน Facebook
2. แสดงความคิดเห็น
3. ระบบจะ:
   - ตอบกลับคอมเมนต์ (ถ้าเปิดใช้งาน)
   - ส่งข้อความทาง Messenger (ถ้าเปิดใช้งาน)
4. ตอบกลับข้อความ → AI จะตอบโดยเข้าใจบริบทจากโพสต์

## 🏗️ สถาปัตยกรรม

### Database Models

1. **MessengerConversation** - เก็บประวัติการสนทนา
2. **FacebookPost** - เก็บข้อมูลโพสต์และการตั้งค่า
3. **FacebookComment** - เก็บข้อมูลคอมเมนต์
4. **FacebookAutomationLog** - เก็บ logs การทำงาน

### API Endpoints

#### Admin APIs
- `GET /api/admin/facebook-automation/posts` - ดึงรายการโพสต์
- `POST /api/admin/facebook-automation/posts` - สร้าง/อัพเดทโพสต์
- `GET /api/admin/facebook-automation/posts/[postId]` - ดึงข้อมูลโพสต์
- `PATCH /api/admin/facebook-automation/posts/[postId]` - อัพเดทการตั้งค่า
- `DELETE /api/admin/facebook-automation/posts/[postId]` - ลบโพสต์
- `GET /api/admin/facebook-automation/comments` - ดึงรายการคอมเมนต์
- `GET /api/admin/facebook-automation/logs` - ดึงรายการ logs
- `GET /api/admin/facebook-automation/stats` - ดึงสถิติ

#### Webhook & Workers
- `POST /api/messenger/webhook` - รับ events จาก Facebook
- `POST /api/worker/facebook-comments` - ประมวลผล comment events
- `POST /api/worker/messenger-v2` - ประมวลผล message events (รองรับบริบท)

### การทำงานของระบบ

```
1. ลูกค้าคอมเมนต์ → Facebook Webhook
                    ↓
2. Comment Worker → ตรวจสอบการตั้งค่า
                    ↓
3. ตอบกลับคอมเมนต์ (ถ้าเปิด)
   ส่งข้อความ Messenger (ถ้าเปิด)
                    ↓
4. บันทึกลง MessengerConversation
   พร้อม source (postId, commentId, context)
                    ↓
5. ลูกค้าตอบกลับ → Messenger Worker V2
                    ↓
6. ดึงประวัติการสนทนา
   แทรกบริบทจาก source
                    ↓
7. ส่งไปยัง OpenAI
                    ↓
8. AI ตอบโดยเข้าใจบริบท
```

## 🔍 การ Debug

### ดู Logs

```bash
# ดู logs ทั้งหมด
tail -f logs/app.log

# ดู logs เฉพาะ Comment Worker
grep "CommentWorker" logs/app.log

# ดู logs เฉพาะ Messenger Worker
grep "MessengerWorkerV2" logs/app.log
```

### ตรวจสอบ Database

```javascript
// ดู Conversations
db.messengerconversations.find({ psid: "PSID_HERE" })

// ดู Posts
db.facebookposts.find({ "automation.enabled": true })

// ดู Logs
db.facebookautomationlogs.find({ status: "failed" }).sort({ createdAt: -1 })
```

## 🚨 Troubleshooting

### ไม่ได้รับ Webhook Events

1. ตรวจสอบ Webhook URL ใน Facebook App
2. ตรวจสอบ Verify Token
3. ตรวจสอบว่า Subscribe Events ครบหรือไม่

### ไม่สามารถส่งข้อความได้

1. ตรวจสอบ `FB_PAGE_ACCESS_TOKEN`
2. ตรวจสอบว่า Page มีสิทธิ์ `pages_messaging`
3. ดู logs ใน `/api/admin/facebook-automation/logs`

### AI ไม่เห็นบริบท

1. ตรวจสอบว่า `source` ถูกบันทึกใน MessengerConversation
2. ตรวจสอบ `aiInstructions` และ `aiContext` ในการตั้งค่าโพสต์
3. ดู logs ใน Messenger Worker V2

## 📊 สถิติและ Monitoring

ดูสถิติได้ที่:
- `/admin/facebook-automation` - สถิติรวม
- `/admin/facebook-automation/[postId]` - สถิติแต่ละโพสต์

สถิติที่แสดง:
- จำนวนโพสต์ทั้งหมด
- จำนวนโพสต์ที่เปิดใช้งาน
- จำนวนคอมเมนต์ทั้งหมด
- จำนวนคอมเมนต์ที่ตอบกลับแล้ว
- จำนวนข้อความที่ส่งแล้ว
- Success/Failed logs

## 🔐 Security

- ✅ ตรวจสอบ Signature จาก Facebook Webhook
- ✅ ใช้ Environment Variables สำหรับ Secrets
- ✅ Admin UI ต้อง Login
- ✅ Rate Limiting (ตาม Facebook API Limits)

## 📝 License

MIT

